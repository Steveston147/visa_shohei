# Merge guidance

This branch should remain unmerged until the generated guarantee letter has been visually checked against the official form and the TypeScript/build commands have passed in a local or CI environment.
