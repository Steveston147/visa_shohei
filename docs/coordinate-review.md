# Coordinate review

The first implementation draws the guarantee letter directly on Canvas. Before production use, print or render one completed PDF and compare it with the official one-page form. Adjust the fixed Canvas coordinates in `src/pdf/renderGuaranteeCanvas.ts` where necessary.
